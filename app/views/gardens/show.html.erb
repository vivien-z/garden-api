<head>
  <script type="text/javascript" scr="mousedown_to_drag.js"></script>
</head>

<div class="container">
  <div class="row">
    <div class="col-xs-12 my-5">
      <h2 class="my-3"><%= @garden.name.capitalize %></h2>
      <section
           class="drop-field garden-field"
           style="height: <%= (@garden.width)*2.to_i %>rem; width: <%= (@garden.length)*2.to_i %>rem"
      >
      </section>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12 my-5">
      <div class="mb-3">
        <h4>My Plant Collection</h4>
        <section id="plantsToDrag" class="drag-target plants-list my-3">
          <% @user.collections.each_with_index do |collection, i| %>
              <!-- ondragstart="mousedownToDrag()" -->
            <div
              id=<%= i %>;
              class="cards-list-ms draggable plantOrigin yellow m-3"
              draggable="true"
              style="height: <%= (collection.plant.size)*2.to_i %>rem;
                     width: <%= (collection.plant.size)*2.to_i %>rem;
                     cursor: copy "
            >
              <%= link_to plant_path(collection.plant) do %>
                <%= collection.plant.name.capitalize %>
              <% end %>
            </div>
          <% end %>
        </section>
      </div>

    </div>
  </div>
</div>
